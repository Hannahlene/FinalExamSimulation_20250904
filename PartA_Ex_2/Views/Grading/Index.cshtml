@{
    ViewData["Title"] = "Student Grading System";
}

<h2 class="text-center">🎓 Grading System Result</h2>

<ul class="list-group mt-3">
    @foreach (var res in ViewBag.Results)
    {
        <li class="list-group-item">@res</li>
    }
</ul>

<p class="mt-3 text-muted">
    <strong>Note:</strong> Undergraduate passes at 50+, Postgraduate passes at 60+.
</p>